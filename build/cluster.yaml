apiVersion: v1
kind: Service
metadata:
  labels:
    name: golangapi-service
  name: golangapi-service
spec:
  selector:
    app: golangapi-service
  ports:
  - protocol: TCP
    port: 50000
    targetPort: 50000
    name: golangapi-hport

