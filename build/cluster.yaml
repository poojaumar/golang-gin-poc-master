apiVersion: v1
kind: Service
metadata:
  labels:
    name: golangapi-service
  name: golangapi-service
spec:
  selector:
    app: golangapi-service
  ports:
  - protocol: TCP
    port: 8000
    targetPort: 8000
    name: golangapi-httpport

